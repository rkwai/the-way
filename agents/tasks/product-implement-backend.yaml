name: "Product: Implement Backend Capabilities"
objective: Ship or stub the APIs and data plumbing defined in the PRD.
inputs:
  - artifacts/product/prd-data-flow.md
  - products/service-api/
steps:
  - Scaffold or update services/endpoints in products/service-api/ to match the data flow table in the PRD.
  - Document migrations or infrastructure scripts required for the data flow between products and systems.
  - Record implementation status and links to commits or PRs in agents/logs/prd-data-flow.md.
evidence:
  - Code or stubs present in products/service-api/ matching the PRD.
  - Implementation notes added to agents/logs/prd-data-flow.md.
